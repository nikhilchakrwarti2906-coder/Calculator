console.log("THere are three types of variables in js that we use \n sothe first one is var  which is globlal it means if we delclre once then we can change it or get it out of scope also  \n the second one is let this tis the most preffered variable because it is local scopr \n thire one is const it is strict datatpe variavle it means if we declare it once then we cant change its vlaue and ots datatype");
